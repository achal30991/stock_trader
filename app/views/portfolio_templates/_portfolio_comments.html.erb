<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Comments

    <a data-toggle="collapse" href="#portfolio_comments_new_form">
      Add comment
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @portfolio.comments.each do |comment| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <a href="/comments/<%= comment.id %>">
          <%= comment.commenter_id %>
        </a>

        <a href="/delete_comment_from_portfolio/<%= comment.id %>">
          Delete
        </a>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="portfolio_comments_new_form">
        <%= render partial: "portfolio_templates/portfolio_comments_new_form" %>
      </div>
    </div>

  </ul>
</div>
